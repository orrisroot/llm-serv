# Log directory for llm-serv@.service instances. Created here rather than with
# LogsDirectory= so that it exists before the unit opens its log files.

#Type Path               Mode UID  GID  Age
d     /var/log/llm-serv  0750 root root -
